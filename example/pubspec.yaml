name: flutter_wallet_example
description: Demonstrates how to use the flutter_wallet plugin.
publish_to: 'none'

environment:
  sdk: ">=2.12.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_wallet:
    path: ../

flutter:
  uses-material-design: true
